const clockDiv = {
  fontSize: "16rem",
  display: "flex",
  alignItems: "center",
  justifyContent: "center"
};

const outerDiv = {
    height: "100vh",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    flexDirection: "column",
};

const spanDiv = {
    fontSize: "3rem",
    marginLeft: "50%"
};

const styles = {
  clockDiv: clockDiv,
  outerDiv: outerDiv,
  spanDiv: spanDiv
};

export default styles;
